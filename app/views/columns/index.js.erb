$('#all-columns').append('<%= j render @columns %>');
<% if @columns.next_page %>
  $('.pagination').replaceWith(
      '<%= j will_paginate @columns,
                           rendered: WillPaginate::ActionView::Bootstrap4LinkRenderer %>'
  );
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>

